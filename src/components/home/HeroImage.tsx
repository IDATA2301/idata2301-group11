import styles from "./HeroImage.module.css";

/** Decorative homepage hero background image. */
export default function HeroImage() {
  return <div className={styles.heroImage} aria-hidden="true" />;
}
